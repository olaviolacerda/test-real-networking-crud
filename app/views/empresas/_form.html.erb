<%= form_for @empresa, url: {action: acao} do |f| %>
  <%= f.label :sua_empresa %> <br>
  <%= f.text_field :nome %>
  <br>
  <%= f.label :setor_onde_atua_sua_empresa %> <br>
  <%= f.collection_select :setor_id, @setors, :id, :nome %>
  <br><br>
  <%= f.submit texto_acao%>
  <% end %>
